# 请帮我生成一个符合 Anthropic 官方标准的 Claude Skill 文件

## 📋 我的需求信息（请填写以下内容）

### 基本信息
- **技能名称**：[用简短英文描述，例如：weekly-report-generator]
- **作者/团队**：[例如：MyTeam]
- **版本号**：[例如：1.0.0]
- **许可证**（可选）：[例如：MIT / Apache-2.0 / 留空]

### 功能描述
- **这个技能做什么**：[用一两句话描述核心功能，例如：根据项目数据自动生成周报]
- **用户会怎么触发它**：[列出用户可能说的话，例如："帮我写周报"、"生成本周工作总结"、"create weekly report"]
- **不应该触发的场景**（可选）：[例如：不用于日报、不用于会议纪要]
- **技能类型**：（单选）对话型 / 内容生成型 / 工作流型
  - 🗣️ 对话型：日常对话即可触发，不需要特定输入格式
  - 📝 内容生成型：用户需要提供主题/素材/文案，AI 会结构化产出
  - 🛠️ 工作流型：用户提供领域上下文，AI 按预设 SOP 执行

### 使用场景
- **具体用例 1**：[描述一个完整的使用场景]
- **具体用例 2**：[描述另一个使用场景]
- **具体用例 3**（可选）：[更多场景]

### 用户使用说明（重要！新技能必须有）
请用一段话描述用户应该如何使用这个技能。参考以下分类：

- **对话型**：直接和 AI 对话即可，触发短语写在 description 里
- **内容生成型**：用户需要提供什么样的输入？请给出参考格式示例
- **工作流型**：用户需要提供什么上下文/参数？

**参考示例**（内容生成型）：
```
你只需要提供：视频脚本 / 文案 / 产品需求
AI 会自动分析时长、格式、风格，按8步管线输出完整故事板

参考输入格式：
[时长：30秒 | 风格：温暖治愈 | 格式：9:16竖屏]
[核心信息：一句话说清楚]

【00:00-00:05】场景描述
- 画面：...
- 旁白/对话：...
```

### 技术细节
- **是否需要 MCP 服务器**：[是/否，如果是请说明哪个服务，例如：Linear、Notion、Slack]
- **是否需要脚本**：[是/否，如果是请说明语言和用途]
- **是否需要参考文档**：[是/否，如果是请简述内容]
- **是否需要资源文件**（模板/图标等）：[是/否]

### 工作流程
请描述技能的主要步骤：
1. [第一步：例如 - 收集用户输入的项目名称和时间范围]
2. [第二步：例如 - 通过 MCP 获取项目数据]
3. [第三步：例如 - 按模板生成周报]
4. [更多步骤...]

### 常见错误/边界情况（可选）
- [例如：用户未提供日期范围时应默认使用本周]
- [例如：API 连接失败时的处理方式]

---

## 🎯 生成要求

请严格按照以下规范生成完整的 Skill 文件结构：

### 1. 文件夹结构
输出推荐的文件夹结构（kebab-case 命名，无空格/下划线/大写）。

### 2. SKILL.md 文件（核心）
必须包含：
- **YAML Frontmatter**（用 `---` 包裹）：
  - `name`：kebab-case，与文件夹同名
  - `description`：包含「做什么」+「何时使用」+「触发短语」，不超过 1024 字符，禁止使用 XML 标签 `< >`
  - 可选字段：`license`、`compatibility`、`metadata`（author/version/mcp-server 等）
- **正文指令**（Markdown 格式）：
  - 技能概述
  - 【必填】使用入口 / 用户需要提供什么？— 按 skill 类型写清楚用户如何调用
  - 分步骤的操作指令（具体、可执行）
  - 使用示例（含用户输入 → 执行动作 → 预期结果）
  - 错误处理/故障排除
  - 性能注意事项

### 3. 质量检查清单
生成后请对照验证：
- [ ] 文件夹 kebab-case 命名
- [ ] 文件名严格为 `SKILL.md`（大小写敏感）
- [ ] YAML 有 `---` 分隔符
- [ ] name 字段 kebab-case，无空格无大写
- [ ] description 同时包含 WHAT 和 WHEN
- [ ] 无 XML 标签（`<` `>`）
- [ ] 无 README.md 文件
- [ ] 指令清晰可执行
- [ ] 包含错误处理
- [ ] 引用文件路径正确
- [ ] SKILL.md 正文控制在 5000 词以内
- [ ] **有「使用入口」章节** — 用户知道怎么调用这个 skill